#pragma once

#include <string>

namespace gp
{
    namespace modifiers {
        typedef std::string ModifierType;
        namespace types {
            const ModifierType UndefinedModifier = "---";
        }
    }
}
